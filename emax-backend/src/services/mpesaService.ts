export const mpesaService = {
  async getAccessToken() {},

  async stkPush(
    phone: string,
    amount: number,
    orderNumber: string
  ) {},

  async query(checkoutRequestId: string) {},

  async handleCallback(data: any) {}
};